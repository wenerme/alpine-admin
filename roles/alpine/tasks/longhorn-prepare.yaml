- name: install deps
  become: true
  apk:
    name: "{{item}}"
  loop:
  - open-iscsi
  - curl
  - findmnt
  - grep
  - gawk
  - blkid
  - lsblk
  - util-linux

- name: start iscsi
  become: true
  service:
    name: iscsid
    state: started
    enabled: true
